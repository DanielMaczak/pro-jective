import { Button } from 'irmas-preact-form-components';

export function ZoomInOutControl() {
  return (
    <>
      <Button value="-" action={() => {}} />
      <Button value="+" action={() => {}} />
    </>
  );
}
