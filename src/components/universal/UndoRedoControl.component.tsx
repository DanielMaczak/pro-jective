import { Button } from 'irmas-preact-form-components';

export function UndoRedoControl() {
  return (
    <>
      <Button value="Undo" action={() => {}} />
      <Button value="Redo" action={() => {}} />
    </>
  );
}
